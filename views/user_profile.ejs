<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
</head>
<body>
    <h1>hey lode we are in user</h1>
    <!-- here this user is set in res.locals during 
    if(req.isAuthenticated())
    {
    res.locals.user=req.user  
   }
  // so here we can acces user info using res.local.user
    -->
  <p><%=user.email%></p>
  <p><%=user.name%></p>
</body>
</html>